language: bash

before_install:
        - sudo apt-get update && sudo apt-get install -y shellcheck

script:
        - ls -l
        - bash test.sh
        - ls -l
        - pwd
        - cat 0x04/ImageProvessing.sh
        - cat 0x04/WorldCup.sh
        - cat 0x04/VisLog.sh

notifications:
        email:
                on_success: never

